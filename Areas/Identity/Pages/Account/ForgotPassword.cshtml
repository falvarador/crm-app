@page
@model ForgotPasswordModel

@{
    ViewData["Title"] = "Forgot your password?";
}

<main class="app-main flex-grow-1 d-flex align-items-center justify-content-center px-3 px-sm-4 py-5">
    <section class="app-shell w-100" aria-labelledby="forgot-password-title">
        <header class="app-header text-center">
            <div class="app-brand mx-auto mb-3 mb-md-2" aria-hidden="true">
                <span class="app-brand-mark">
                    <i class="bi bi-person-square"></i>
                </span>
            </div>

            <h1 id="forgot-password-title" class="app-title mb-0">Reset your password</h1>
        </header>
        
        <article class="app-card card border-0 mx-auto mt-4 mt-md-3">
            <div class="card-body app-card-body">
                <form method="post" class="app-form" novalidate>
                    <div asp-validation-summary="ModelOnly" class="app-summary text-danger" role="alert"></div>
                    
                    <div class="auth-field mb-4">
                        <label asp-for="Input.Email" class="form-label app-label"></label>
                        <input asp-for="Input.Email"
                               type="email"
                               class="form-control app-control"
                               autocomplete="email"
                               placeholder="name@company.com" />
                        <span asp-validation-for="Input.Email" class="app-feedback text-danger"></span>
                    </div>
                    
                    <button class="btn btn-primary app-submit w-100" type="submit">
                        Reset Password
                    </button>
                    
                    <hr class="app-divider" />

                    <nav class="app-links text-center" aria-label="Account recovery and registration links">
                        <p class="mb-0">
                            <a asp-page="./Login" class="app-link">Return to login</a>
                        </p>
                    </nav>
                </form>
            </div>
        </article>
    </section>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
